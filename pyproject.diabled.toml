[project]
name = "olist-ai-analyst"
version = "0.2.0"
description = "AI analytics workspace for natural-language querying over a Supabase/Postgres warehouse."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.dagster]
module_name = "orchestration.dagster.definitions"
code_location_name = "ai_business_analyst"